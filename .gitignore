# Runtime noise: verbose, rewritten constantly, no value in history.
trader.log
supervisor.log
STOP
__pycache__/

# The trading record IS the deliverable, so trade_log.csv, state_*.json and
# exclusions_cache.json are tracked and snapshotted by snapshot.sh.
